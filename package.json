{
  "name": "assignment-task",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend/app",
    "build": "npm run build --prefix frontend/app",
    "preview": "npm run preview --prefix frontend/app",
    "backend": "npm start --prefix backend",
    "install-all": "npm install && npm install --prefix frontend/app && npm install --prefix backend"
  }
}
